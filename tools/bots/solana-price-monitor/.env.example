# Telegram Bot Configuration
# Required: Bot token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Access mode: private | public | community
# - private: Only ALLOWED_USER_IDS can use the bot (default)
# - public: Anyone can use the bot, no admin features
# - community: Anyone can use, admins have /stats and /broadcast
TELEGRAM_MODE=private

# User access control (comma-separated Telegram user IDs)
# Required for private mode, optional for community mode
ALLOWED_USER_IDS=123456789

# Admin user IDs for community mode (comma-separated)
# These users get access to /stats and /broadcast commands
TELEGRAM_ADMIN_IDS=123456789

# Logging
# Levels: debug | info | warn | error
LOG_LEVEL=info

# Monitoring settings
# Price polling interval in milliseconds (default: 30000 = 30 seconds)
POLL_INTERVAL_MS=30000

# Maximum monitors per user (default: 10)
MAX_MONITORS_PER_USER=10

# Rate limiting
# Maximum commands per minute per user (default: 20)
RATE_LIMIT_PER_MINUTE=20

# Storage
# Directory for state files (default: ./data)
DATA_DIR=./data

# Health check
# Port for HTTP health endpoint (default: 8080)
HEALTH_CHECK_PORT=8080
